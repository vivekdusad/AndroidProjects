import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    home: Scaffold(
      backgroundColor: Colors.white,
        floatingActionButton: FloatingActionButton(
          child: Text("hello"),
        ),
        body: Image.asset("assets/undraw_secure_login_pdn4.png")
        ),

  ));
}
